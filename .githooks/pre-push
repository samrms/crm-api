#!/bin/sh
# Runs the same gates as CI (lint, typecheck, format, tests) before any push.
# Enable once per clone:  git config core.hooksPath .githooks
set -e
echo "pre-push: running checks and tests..."
bun run prepush
